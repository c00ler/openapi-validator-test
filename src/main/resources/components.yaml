components:
  schemas:
    User:
      $ref: 'models/user.yaml'
  parameters:
    userId:
      name: userId
      in: path
      description: ID of the user to use
      required: true
      schema:
        type: integer
        format: int32
